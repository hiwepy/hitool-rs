//! `cn.hutool.core.lang.ref` 子包对齐
//!
//! 自动生成的模块入口,1:1 镜像 Java 包结构。
//! 每个子模块对应一个 Java 类(`.java` → `.rs`),命名遵循 snake_case。
//! 详细对齐信息见各 `.rs` 文件头注释。

pub mod phantom_obj;
pub mod ref_;
pub mod reference_type;
pub mod soft_obj;
pub mod strong_obj;
pub mod weak_obj;
